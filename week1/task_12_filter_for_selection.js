/* eslint-disable no-console */
// filter (print only even number from the array)
const oldarr = [1, 2, 3, 4, 5, 6, 7, 8];
console.log(oldarr);
const newarr = oldarr.filter((item) => item % 2 === 0);
console.log(newarr);
